<UserControl x:Class="MyQs.Wpf.Views.MessageContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid HorizontalAlignment="Left" Margin="0 10 0 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0">Message content</Label>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" Grid.Row="1" Grid.Column="0">
            <TextBox Name="Body" IsReadOnly="True" />
        </ScrollViewer>
    </Grid>
</UserControl>
